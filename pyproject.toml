[project]
name = "rag-pipeline-poc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "faiss-cpu>=1.14.3",
    "sentence-transformers>=5.6.0",
]
